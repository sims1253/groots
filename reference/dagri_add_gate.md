# Add a gate to a dagriculture graph

Add a gate to a dagriculture graph

## Usage

``` r
dagri_add_gate(graph, edge_id, id = NULL, metadata = list())
```

## Arguments

- graph:

  A `dagri_graph`.

- edge_id:

  Edge ID.

- id:

  Optional Gate ID.

- metadata:

  Gate metadata.
