# Contributing

1. Create a topic branch from `main`.
2. Add or update tests with each code change.
3. Run `devtools::document()`, `devtools::build_readme()`, `devtools::build_vignettes()`, `devtools::test()`, and `devtools::check()` before opening a PR.
4. Keep `README`, `NEWS`, and vignettes aligned with user-facing changes.
